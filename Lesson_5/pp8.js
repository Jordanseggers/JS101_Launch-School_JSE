//output all vowels from the strings


let obj = {
  first: ['the', 'quick'],
  second: ['brown', 'fox'],
  third: ['jumped'],
  fourth: ['over', 'the', 'lazy', 'dog'],
};

function outputVowels (inputObj) {
  let theArray = object.values(inputObj);
  theArray.forEach(innerArr => {
    let character = innerArr.split('');
    character.forEach(letter => {
      if letter === 
    })
  }
}